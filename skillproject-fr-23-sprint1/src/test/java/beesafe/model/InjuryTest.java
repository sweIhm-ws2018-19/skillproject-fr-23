/*package test.java.beesafe.model;


import static org.junit.Assert.assertEquals;
import org.junit.Test;

import main.java.beesafe.model.Injury;

public class InjuryTest {
	 @Test
	 public void validateSettersAndGetters() throws Exception{
		 	/* 
		 	com.openjo... lassen sich bei mir nicht importieren 
		 	
	        PojoClass activityPojo = PojoClassFactory.getPojoClass(Injury.class);

	        Validator validator = ValidatorBuilder.create()
	                // Lets make sure that we have a getter and a setter for every field defined.
	                .with(new SetterMustExistRule()).with(new GetterMustExistRule())

	                // Lets also validate that they are behaving as expected
	                .with(new SetterTester()).with(new GetterTester()).build();

	        // Start the Test
	        validator.validate(activityPojo);
	        */
	    /*}
	 
	    @Test
	    public void validateIsValid() throws Exception {
	        Injury injury_null = new Injury(null);
	        assertEquals("isNotValid_null", false, injury_null.isValid());
	        Injury injury_empty = new Injury("");
	        assertEquals("isNotValid_empty", false, injury_empty.isValid());
	        Injury injury = new Injury("stich");
	        assertEquals("isValid", true, injury.isValid());
	}
}*/
